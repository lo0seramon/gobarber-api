import CreateAppointmentService from "./CreateAppointmentService";

describe('CreateAppointment', () => {
    it('should be able to create an appointment', () => {
        expect(1 + 2).toBe(3);
    });

    it('should not be able to create two appointments on same time', () => {
        expect(1 + 2).toBe(3);
    });
});
